spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotels
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          jdbc:
            time_zone: UTC
    show-sql: true

